[project]
name = "workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "flask-cors>=6.0.1",
    "flask>=3.1.1",
    "requests>=2.32.4",
]
